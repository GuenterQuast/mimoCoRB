import mimocorb # from this directory
import sys

from setuptools import setup

setup(
    name='mimocorb',
    version=mimocorb.__version__,
    author='Guenter Quast',
    author_email='Guenter.Quast@online.de',
    packages=['mimocorb'],
    install_requires=[],
    scripts=[],
    classifiers=[
    'Development Status :: 5 - stable',
    # Specify the Python versions you support here. In particular, ensure
    # that you indicate whether you support Python 2, Python 3 or both.
    'Programming Language :: Python :: 3.9',
    ],
    url='http://www.etp.kit.edu/~quast/',
    license='MIT BSD 2-Clause License',
    description='multi-in multi-out Configurable Ring Buffer for DAQ',
    long_description=open('README.md').read(),
    setup_requires=[\
        "NumPy >= 1.20",
        "SciPy >= 1.10",
        "matplotlib >= 3.0",
        "pyarrow >= 10.0",
    ]
)


